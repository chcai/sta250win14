# source in script with functions
source('method1fun.R')

method1.fun(direc = 'data', # folder containing csv files
            dir.out = 'tables') # (existing) folder to store output 
                                # freq tables
